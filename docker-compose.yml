version: '3'
services:
  application:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: counter-pro-bot
    ports:
      - "3000:3000"
    restart: always

  # website:
  #   build:
  #     context: ./website
  #     dockerfile: Dockerfile
  #   container_name: counter-pro-website
  #   ports:
  #     - "80:80"
  #   restart: always
